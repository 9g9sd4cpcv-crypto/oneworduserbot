services:
  - type: worker
    name: userbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    healthCheckPath: /
    regions:
      - frankfurt
    plan: free
